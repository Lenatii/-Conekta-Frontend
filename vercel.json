{
  "buildCommand": "pnpm install && pnpm run build",
  "outputDirectory": "dist/client",
  "framework": "vite",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
